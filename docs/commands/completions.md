# `completions` Command

Generate shell completion script content and print to stdout.

## Supported Shells

- `bash`
- `zsh`
- `fish`
- `powershell`
- `elvish`

## Examples

```bash
99problems completions bash
99problems completions zsh
99problems completions powershell
```

Redirect output into your shell completion directory based on your shell setup.
