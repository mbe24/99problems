# `skill` Command

Initialize the canonical `99problems` agent skill scaffold.

## Subcommands

### `skill init`

Create skill files under `.agents/skills/99problems` by default.

```bash
99problems skill init
```

Override target path:

```bash
99problems skill init --path ~/.agents/skills
```

Force overwrite of generated files:

```bash
99problems skill init --force
```
